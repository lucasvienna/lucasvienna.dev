language: ruby
rvm:
  - 2.6
cache: bundler

before_install:
  - gem update --system
  - gem install bundler

script:
  - bundle exec jekyll build

deploy:
  provider: pages
  local_dir: './_site/'
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
